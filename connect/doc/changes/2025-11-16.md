### Fixed

- A call is registered once **every** channel has ended, not when the first one
  does. Multi-leg calls were being written to the log while part of the
  conversation was still up, so duration and outcome could be wrong.
