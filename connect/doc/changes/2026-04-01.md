### Added

- **SIP REFER**, so a transfer started on a SIP desk phone is carried out by
  Connect instead of being ignored.

### Changed

- Call prices are fetched only by the scheduled job, and a price that cannot be
  retrieved is retried a bounded number of times instead of being chased on
  every call.
