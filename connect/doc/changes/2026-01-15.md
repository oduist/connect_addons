### Added

- **Secure Media** on SIP domains, so the audio leg can be encrypted.

### Changed

- The default call-duration limit is two hours. It was unset before, which left
  a stuck call running indefinitely. An explicit `call_duration_limit` still
  wins.
