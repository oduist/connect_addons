### Fixed

- Messaging is only offered where Twilio actually supports it. Outside the US1
  region the SMS configuration and caller-ID operations are sent to the US
  endpoint instead of failing against a regional one.
