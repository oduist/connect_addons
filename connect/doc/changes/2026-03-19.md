### Added

- **PBX Groups.** A group shares call history among its members, so colleagues
  covering for each other see the same calls without being given access to
  everyone's.

### Fixed

- Partner matching by phone number uses the sanitised number, which fixes
  contacts not being found when the number was stored in a local format.
