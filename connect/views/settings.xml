<?xml version="1.0" encoding="utf-8"?>
<odoo>

  <record model="ir.actions.server" id="connect_settings_action">
      <field name="type">ir.actions.server</field>
      <field name="name">General Settings</field>
      <field name="state">code</field>
      <field name="model_id" ref="model_connect_settings"/>
      <field name="code">action = model.open_settings_form()</field>
  </record>

  <menuitem id="settings_menu" parent="connect.connect_settings_menu"
      groups="connect.group_connect_admin"
      action="connect_settings_action" name="General" sequence="70"/>

  <record id='connect_settings_form' model='ir.ui.view'>
      <field name='name'>connect.settings.form</field>
      <field name='model'>connect.settings</field>
      <field name='type'>form</field>
      <field name='arch' type='xml'>
        <form create="false" delete="false">
            <sheet>
              <notebook>
                <page name="general" string="General">
                  <group>
                    <group>
                      <field name="api_url" string="Odoo URL"/>
                      <button class="btn btn-primary" colspan="2" name="action_open_system_parameters" type="object" string="Change Odoo URL"/>
                      <field name="api_fallback_url" string="Fallback URL"/>
                      <field name="enable_auto_reload_view"/>
                      <field name="debug_mode"/>
                    </group>
                    <group string="Balance">
                      <field name="twilio_balance" string="Current Balance" readonly="1"/>
                      <button class="btn btn-info" colspan="2" name="get_twilio_balance" type="object" string="GET TWILIO BALANCE"/>
                    </group>
                  </group>
                </page>
                <page name="keys" string="API Keys">
                  <group>
                    <group string="Twilio" name="twilio_api">
                       <div colspan="2">
                         <small><strong>You can find your Twilio API data in <a href="https://console.twilio.com/" target="_blank">your Twilio Console</a></strong></small>
                       </div>
                       <field name="twilio_auto_sync" groups="base.group_no_one"/>
                       <field name="account_sid"/>
                       <field name="display_auth_token" string="Auth Token" password="1"/>
                       <field name="twilio_region" string="Region"/>
                       <field name="display_region_auth_token" string="Region Auth Token" password="1" attrs="{'invisible': [('twilio_region', '=', 'us1')], 'required': [('twilio_region', '!=', 'us1')]}"/>
                       <field name="twilio_api_key" string="API Key SID"/>
                       <field name="display_twilio_api_secret" string="API Key Secret" password="1"/>
                       <field name="twilio_edge" string="Edge"/>
                       <button colspan="2" name="sync" type="object" string="SYNC TWILIO ACCOUNT" class="btn btn-primary"/>
                     </group>
                    <group name="openai_api" string="OpenAI">
                      <div colspan="2">
                        <small><strong>You can find your OpenAI API key in <a href="https://platform.openai.com/settings/organization/api-keys" target="_blank">your OpenAI Setings</a></strong></small>
                      </div>
                      <field name="display_openai_api_key" string="OpenAI API Key" password="1"/>
                    </group>
                  </group>
                </page>
                <page name="calls" string="Calls">
                  <group>
                    <group >
                      <field name="system_voice"/>
                      <field name="proxy_recordings"/>
                      <small colspan="2">If you disable it remove the HTTP AUTH in Twilio. Read <a target="new" href="https://help.twilio.com/articles/15827821586843">more</a>.</small>
                      <field name="fetch_call_prices"/>
                      <small colspan="2">Fetch call prices from Twilio API after call completion. This may add delay to call processing.</small>
                    </group>
                    <group>
                      <button colspan="2" name="reformat_numbers_button" string="FORMAT NUMBERS" type="object"/>
                    </group>
                  </group>
                </page>
                <page name="s3_storage" string="S3 Storage">
                  <group>
                    <group string="AWS S3" name="aws_s3">
                      <field name="s3_recordings_enabled"/>
                      <separator string="1. Choose a bucket prefix (the IAM policy below is scoped to it)" colspan="2" attrs="{'invisible': [('s3_recordings_enabled', '=', False)]}"/>
                      <field name="aws_s3_bucket_prefix" attrs="{'invisible': [('s3_recordings_enabled', '=', False)]}"/>
                      <separator string="2. Create an AWS IAM user with this least-privilege policy" colspan="2" attrs="{'invisible': [('s3_recordings_enabled', '=', False)]}"/>
                      <field name="aws_iam_policy" nolabel="1" colspan="2" widget="ace" options="{'mode': 'javascript'}" readonly="1" attrs="{'invisible': [('s3_recordings_enabled', '=', False)]}"/>
                      <div colspan="2" class="text-muted" attrs="{'invisible': [('s3_recordings_enabled', '=', False)]}">
                        <p>Create the IAM user (one time):</p>
                        <ol>
                          <li>AWS Console → <strong>IAM → Users → Create user</strong> (e.g. <code>connect-s3</code>).</li>
                          <li>On <strong>Set permissions</strong> pick <strong>Attach policies directly</strong> → <strong>Create policy</strong> → <strong>JSON</strong> tab → paste the policy above → name it (e.g. <code>connect-s3-recordings</code>) → create, then attach it to the user.</li>
                          <li>Open the user → <strong>Security credentials</strong> → <strong>Create access key</strong> → <em>Application running outside AWS</em> → copy the <strong>Access Key ID</strong> and <strong>Secret access key</strong>.</li>
                          <li>Paste them below.</li>
                        </ol>
                      </div>
                      <separator string="3. Paste the IAM user's access key" colspan="2" attrs="{'invisible': [('s3_recordings_enabled', '=', False)]}"/>
                      <field name="aws_access_key_id" attrs="{'invisible': [('s3_recordings_enabled', '=', False)]}"/>
                      <field name="display_aws_secret_access_key" string="AWS Secret Access Key" password="1" attrs="{'invisible': [('s3_recordings_enabled', '=', False)]}"/>
                      <field name="aws_region" attrs="{'invisible': [('s3_recordings_enabled', '=', False)]}"/>
                      <field name="aws_s3_bucket" placeholder="recordings-acme" attrs="{'invisible': [('s3_recordings_enabled', '=', False)]}"
                             help="Enter a name (or just a suffix). The prefix above is combined with it to form the full bucket name shown below."/>
                      <field name="aws_s3_bucket_name" readonly="1" attrs="{'invisible': [('s3_recordings_enabled', '=', False)]}"/>
                      <field name="aws_s3_prefix" attrs="{'invisible': [('s3_recordings_enabled', '=', False)]}"/>
                      <field name="s3_retention_days" attrs="{'invisible': [('s3_recordings_enabled', '=', False)]}"/>
                      <button colspan="2" name="action_provision_s3_bucket" type="object" string="CREATE / CONFIGURE S3 BUCKET" class="btn btn-primary" attrs="{'invisible': [('s3_recordings_enabled', '=', False)]}"/>
                      <div colspan="2" attrs="{'invisible': [('s3_recordings_enabled', '=', False)]}">
                        <button name="action_create_twilio_aws_credential" type="object" string="CREATE TWILIO AWS CREDENTIAL" class="btn btn-secondary me-2"/>
                        <button name="action_recreate_twilio_aws_credential" type="object" string="RECREATE TWILIO CREDENTIAL" class="btn btn-secondary"
                                confirm="Delete the existing 'connect-s3-recordings' Twilio credential and create a new one with the current AWS keys? The new SID must be re-selected in the Twilio Console."/>
                      </div>
                      <field name="twilio_aws_credential_sid" readonly="1" attrs="{'invisible': [('s3_recordings_enabled', '=', False)]}"/>
                      <field name="aws_s3_url" readonly="1" attrs="{'invisible': [('s3_recordings_enabled', '=', False)]}"/>
                    </group>
                    <group string="Twilio Console (final step)" name="s3_instructions" attrs="{'invisible': [('s3_recordings_enabled', '=', False)]}">
                      <div colspan="2" class="text-muted">
                        <p><strong>Voice external storage has no API — do this once in the Twilio Console:</strong></p>
                        <ol>
                          <li>Twilio Console → Voice → Recordings → Settings.</li>
                          <li>Enable external S3 storage.</li>
                          <li>Pick AWS credential <em>connect-s3-recordings</em> (SID shown on the left).</li>
                          <li>Paste the <strong>S3 URL</strong> shown on the left.</li>
                          <li>Save. New recordings now go to your bucket.</li>
                          <li>Then tick <em>Store recordings in S3</em> here.</li>
                        </ol>
                      </div>
                    </group>
                  </group>
                </page>
                <page name="transcription" string="Transcription">
                  <group>
                    <group name="transcribe_options">
                      <field name="transcript_calls"/>
                      <field name="transcript_provider"
                             attrs="{'invisible': [('transcript_calls', '=', False)]}"/>
                      <field name="summary_prompt"
                             attrs="{'invisible': [('transcript_calls', '=', False)]}"/>
                      <field name="register_summary"
                             attrs="{'invisible': [('transcript_calls', '=', False)]}"/>
                      <field name="transcript_voice_message" string="Transcribe Voice Messages"/>
                   </group>
                    <group string="Transcribe Rules"
                           attrs="{'invisible': [('transcript_calls', '=', False)]}">
                        <field name="transcription_rules" nolabel="1" colspan="2">
                            <tree editable="bottom">
                                <field name="calling_number"/>
                                <field name="called_number"/>
                            </tree>
                        </field>
                    </group>
                  </group>
                </page>
                <page name="chatter" string="Chatter">
                    <group>
                        <group>
                            <field name="chatter_message_generate_prompt"/>
                        </group>
                    </group>
                </page>
                <page string="Development" groups="base.group_no_one">
                  <group>
                    <group>
                      <field name="twilio_verify_requests"/>
                    </group>
                  </group>
                </page>

              </notebook>
            </sheet>
        </form>
      </field>
  </record>

</odoo>
