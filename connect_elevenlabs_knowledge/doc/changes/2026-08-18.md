### Added

- `user_guide.md`: first end-user guide — the knowledge library, the
  draft/creating/active/error lifecycle and what to check when an agent ignores
  a document, View Agents, the agent's Knowledge Base tab, and the supported
  file formats.
- `admin_guide.md`: first administrator guide — adding documents as file, URL or
  text with the format restriction, attaching them to agents from either side,
  the SYNC KNOWLEDGE button, deletion including the force-delete flag, guidance
  on what makes good knowledge-base content, and a health check.
