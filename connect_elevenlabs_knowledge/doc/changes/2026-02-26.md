### Fixed

- The document-kind field was named `type`, which is reserved, and clashed on
  the knowledge form and on the agent's Knowledge Base tab. It is now
  `document_type`. Its three values -- URL, File and Text -- are unchanged.
