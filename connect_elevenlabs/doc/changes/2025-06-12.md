### Changed

- The post-call webhook verifies the ElevenLabs signature before it accepts
  anything, using the shared secret and a timestamp check. Unsigned or stale
  posts to that endpoint are rejected instead of being processed.
