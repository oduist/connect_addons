### Added

- `user_guide.md`: first end-user guide — what a call handled by an AI agent
  looks like in the call log (agent, summary, transcript, audio player), being
  transferred by an agent to a person, ElevenLabs voices in call flows and
  voicemail, and what is administrator-only.
- `admin_guide.md`: first administrator guide — connecting the ElevenLabs
  account and what each of the SYNC / SYNC TOOLS / REGENERATE PROMPTS / UNBIND
  buttons does, the post-call webhook, the agent service and its ping, creating
  an agent (prompt, conversation envelope, LLM/TTS parameters), tools and their
  parameters, pointing numbers and extensions at an agent, per-user voicemail
  files, the license check, a cost-control section, and a post-setup health
  check.
