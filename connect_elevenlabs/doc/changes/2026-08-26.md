### Changed

- `static/description/index.html`: rebuilt in the Oduist "Aurora" style the
  marketing site uses -- a dark panel painting its own ground, cyan mono kickers,
  a feature-card grid, and the brand gradient kept to the four places the palette
  sanctions. The feature cards come from this module's own guides and code, so
  the Apps listing describes what it does.

- The page it replaces was a copy of the `connect` store page: it advertised the
  core telephony platform rather than the ElevenLabs voice agents this module
  adds.
