### Fixed

- The **Recordings** list showed the generic summary even for calls an agent
  handled. It now prefers the agent's own summary when there is one, so the
  column matches what is on the record.
