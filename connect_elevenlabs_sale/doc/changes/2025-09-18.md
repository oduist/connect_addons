### Added

- The module, letting the voice AI agent work with Sales: list the catalogue,
  create a sales order, read a caller's orders back to them, and create a
  contact for a caller who is not in the database yet -- each behind its own
  tool and endpoint.
