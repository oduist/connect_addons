### Changed

- The module's endpoints check the ElevenLabs agent token and refuse anything
  else, the same gate the other agent tools use. The refusal arrives as a
  jsonrpc error envelope inside a `200`, not as an HTTP `401`.
