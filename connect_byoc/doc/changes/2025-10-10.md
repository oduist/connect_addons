### Changed

- BYOC calls honour the call-duration limit. The `call_duration_limit` setting
  is passed to the carrier leg as Twilio's `timeLimit`, so a limit configured in
  Connect now applies to calls leaving through your own carrier too, not only to
  Twilio-routed ones.
