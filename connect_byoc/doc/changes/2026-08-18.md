### Added

- `user_guide.md`: first end-user guide — what BYOC changes for a user (almost
  nothing), BYOC-type outgoing caller IDs, and why outgoing rules can make a
  destination dial differently.
- `admin_guide.md`: first administrator guide — the BYOC trunk record with its
  origination URIs, priority/weight, SIP credentials and computed callback URLs,
  the Sync action, BYOC caller IDs and numbers, outgoing rules with
  longest-prefix matching and the shipped "All destinations" rule, a setup order
  and a health check.
