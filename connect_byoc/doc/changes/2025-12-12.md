### Added

- **Number manipulation on outgoing rules.** A rule can add a prefix to the
  dialled number or trim leading digits from it before the call is handed to the
  carrier. This is what lets you keep entering numbers in international format
  while a carrier that expects a national `0`, or no country code, still
  receives what it wants.
