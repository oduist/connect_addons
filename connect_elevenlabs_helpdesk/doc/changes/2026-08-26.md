### Added

- `static/description/index.html`: a store page for the first time. It follows
  the Oduist "Aurora" style the marketing site uses -- a dark panel painting its
  own ground, cyan mono kickers, a feature-card grid, and the brand gradient kept
  to the four places the palette sanctions. The feature cards come from this
  module's own guides and code, so the Apps listing describes what it does.

- `static/description/icon.png`: the shared Connect icon, which this module did
  not carry before.

### Fixed

- The manifest pointed `images` at a `static/description/logo.png` that does not
  exist in this module, so the Apps listing had a broken preview. It now points
  at the shared icon.
