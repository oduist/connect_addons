### Added

- `user_guide.md`: first end-user guide — the five things the agent can do with
  tickets, what an agent-created ticket carries (linked call, transcript,
  summary), and how to treat tickets the agent got wrong.
- `admin_guide.md`: first administrator guide — the five shipped tools with
  their endpoints and required parameters, the Helpdesk Support Agent template,
  the setup sequence including SYNC TOOLS, guidance on which write-capable tools
  to attach, the team-name matching caveat, and a health check.
