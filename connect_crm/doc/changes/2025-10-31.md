### Added

- A lead can be created from an incoming **message**, not only from a call. An
  SMS or WhatsApp message from a number with no lead behind it opens one,
  carrying that number, so a conversation that starts in writing lands in the
  pipeline the same way a call does.
