### Fixed

- Activating Connect CRM on Odoo 19 failed with an RPC error: Odoo 19 no longer
  carries `mobile` on the lead, which the module's search relies on. The field
  is restored on Odoo 19 and up, so leads are again found by mobile number.
