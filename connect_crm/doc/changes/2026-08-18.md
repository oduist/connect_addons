### Added

- `user_guide.md`: first end-user guide — the Calls button on a lead, finding a
  lead by phone or mobile, how a call gets linked to a lead and how to correct
  it by hand, what leads created from calls look like, and call source
  attribution.
- `admin_guide.md`: first administrator guide — the CRM tab of the Connect
  settings, both directions of automatic lead creation with every sub-option,
  the fallback salesperson and lead/opportunity choice, guidance for picking a
  combination, the fields added to calls and leads, UTM campaign attribution,
  and the CRM Lead messaging destination.
