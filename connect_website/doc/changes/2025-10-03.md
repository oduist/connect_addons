### Changed

- The website block is now called **Connect Button** and its template is
  `s_connect_button`; it was `s_talk_button` before. A page that already had the
  old block keeps working, but the block is found under the new name in the
  editor.
