### Added

- `user_guide.md`: first end-user guide — the "Let's Talk" button from the
  visitor's side, what a website call looks like for the team answering it,
  adding the snippet to a page, and the three usual failure symptoms.
- `admin_guide.md`: first administrator guide — the Website settings tab
  (enable, extension, domain) and why the destination fields are mandatory,
  placing the snippet, how website calls are routed and why callers are
  anonymous by default, the HTTPS requirement, the `connect_website` license
  and its misleading failure mode, and a health check.
