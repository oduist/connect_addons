name: CI
on:
  push:
    branches:
      - 18.0
      - 19.0

jobs:
  Install:
    runs-on: ubuntu-22.04
    container:
      image: odoo:${{ github.ref_name }}
      options: --user=root
    services:
      db:
        image: postgres:12
        env:
          POSTGRES_PASSWORD: odoo
          POSTGRES_USER: odoo
    steps:
      - name: Check out repository code
        uses: actions/checkout@master
      - name: Install module requirements
        run: pip3 install twilio elevenlabs openai==1.39.0 typing_extensions==4.10.0 boto3 --break-system-packages
      - name: Install odoo modules
        run: >
          odoo --addons-path=$GITHUB_WORKSPACE --database=ephemeral
          --db_host=db --db_user=odoo --db_password=odoo
          --stop-after-init --no-http
          -i connect,connect_crm,connect_helpdesk,connect_elevenlabs
